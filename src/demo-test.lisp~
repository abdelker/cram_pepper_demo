(in-package :pepper-demo)

(defvar ?target nil)
(setq ?target (desig:an object
  (type object)
  (name "cube_GBCG")))


(defun look-at ()
(let((?targettest ?target))
  (princ ?targettest)
  (top-level
    (with-process-modules-running (pepper-navigation)
      (let ((target (desig:a motion (type looking) (target (desig:an object
  (type object)
  (name "cube_GBCG")))))))
         (pm-execute 'pepper-navigation target))))))